#include<iostream>
#include<vector>
#define ll long long int 
using namespace std;

void solve()
{
    vector<string>s (3);
    for(int i=0; i<3; i++)
    {
        cin >> s[i];
    }
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie();
    cout.tie();
    ll t; cin >> t;
    while(t--)
    {
        solve();
        printf("\n");
    }
}